---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";

const allJournalPosts = (await getCollection("blog")).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const allResearchPosts = (await getCollection("research")).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const allReadingPosts = (await getCollection("reading")).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const allMusicPosts = (await getCollection("music")).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const allNotesPosts = (await getCollection("notes")).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const allProjects = (await getCollection("projects")).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const featuredProjects = allProjects
    .filter((p: CollectionEntry<"projects">) => p.data.featured)
    .slice(0, 3);

// ══ 首頁右上「最近更新」：跨全部 collection 找最新的一筆 ═══════════
// 圖片走退路階梯：heroImage → 內文第一張圖 → coverImage → YouTube → 星空。
// 每一層都可能是空的，所以最後一定要有星空接住，不會開天窗。
function firstBodyImage(body: string | undefined): string | undefined {
    if (!body) return undefined;
    const m = body.match(/!\[[^\]]*\]\(([^)]+)\)/);
    return m ? m[1] : undefined;
}

type Recent = {
    href: string; kind: string; kindEn: string;
    date: Date; title: string; desc: string;
    image?: string; imageKind?: "photo" | "cover" | "youtube";
    youtubeId?: string;
};

const recentCandidates: Recent[] = [
    ...allJournalPosts.map((p) => ({
        href: `/blog/${p.id}`, kind: "生活隨筆", kindEn: "JOURNAL",
        date: p.data.pubDate, title: p.data.title,
        desc: p.data.quote ?? "",
        image: p.data.heroImage ?? firstBodyImage(p.body),
        imageKind: "photo" as const,
    })),
    ...allNotesPosts.map((p) => ({
        href: `/notes/${p.id}`, kind: "學習筆記", kindEn: "NOTES",
        date: p.data.pubDate, title: p.data.title,
        desc: p.data.description ?? "",
    })),
    ...allResearchPosts.map((p) => ({
        href: `/research/${p.id}`, kind: "讀讀文獻", kindEn: "RESEARCH",
        date: p.data.pubDate, title: p.data.title,
        desc: p.data.abstract ?? "",
    })),
    ...allReadingPosts.map((p) => ({
        href: `/reading/${p.id}`, kind: "看看書籍", kindEn: "READING",
        date: p.data.pubDate, title: p.data.title,
        desc: `${p.data.bookTitle} · ${p.data.author}`,
        image: p.data.coverImage, imageKind: "cover" as const,
    })),
    ...allMusicPosts.map((p) => ({
        href: `/music/${p.id}`, kind: "聽聽音樂", kindEn: "MUSIC",
        date: p.data.pubDate, title: p.data.songName,
        desc: p.data.artist,
        image: p.data.youtubeId
            ? `https://img.youtube.com/vi/${p.data.youtubeId}/maxresdefault.jpg`
            : undefined,
        imageKind: "youtube" as const,
        youtubeId: p.data.youtubeId,
    })),
    ...allProjects.map((p) => ({
        href: `/projects/${p.id}`, kind: "作品集", kindEn: "PROJECTS",
        date: p.data.pubDate, title: p.data.title,
        desc: p.data.description,
        image: p.data.coverImage, imageKind: "cover" as const,
    })),
];

const recent = recentCandidates
    .sort((a, b) => b.date.valueOf() - a.date.valueOf())[0];

const recentDateStr = recent
    ? recent.date.toLocaleDateString("zh-TW", { year: "numeric", month: "2-digit", day: "2-digit" })
    : "";

// 星空：一顆星 = 一篇文章。只需要分類和日期，標題用來決定位置。
const skyPosts = recentCandidates.map((r) => ({
    c: r.kindEn,
    t: r.date.valueOf(),
    k: r.title,
}));

const slogans = [
    "想買一桶冰淇淋暴挖起來吃",
    "有一隻貓咪叫做高麗菜",
    "中文字體要什麼才會好看呢",
    "蛋餅蛋餅九層塔蛋餅",
];
const randomSlogan = slogans[Math.floor(Math.random() * slogans.length)];

// ══ 下半儀表板：各分類篇數 ═══════════════════════════════════════
// 資料落差很大（17:1），所以「軌道」等寬滿高可點、「填充」才按篇數等比，
// 並給最矮的保底 14%，讓 2 篇和 1 篇分得出來。比例既然壓過了就不畫刻度線。
const BAR_FLOOR = 14;   // 最矮的百分比
const BAR_TOP = 86;     // 最高的百分比（留空間給頂端讀數，不頂到天花板）

const barSource = [
    { name: "生活隨筆", en: "JOURNAL",  unit: "篇", href: "/blog",     items: allJournalPosts,  latest: allJournalPosts[0]?.data.title },
    { name: "聽聽音樂", en: "MUSIC",    unit: "首", href: "/music",    items: allMusicPosts,    latest: allMusicPosts[0]?.data.songName },
    { name: "作品集",   en: "PROJECTS", unit: "個", href: "/projects", items: allProjects,      latest: allProjects[0]?.data.title },
    { name: "讀讀文獻", en: "RESEARCH", unit: "篇", href: "/research", items: allResearchPosts, latest: allResearchPosts[0]?.data.title },
    { name: "看看書籍", en: "READING",  unit: "本", href: "/reading",  items: allReadingPosts,  latest: allReadingPosts[0]?.data.title },
    { name: "學習筆記", en: "NOTES",    unit: "篇", href: "/notes",    items: allNotesPosts,    latest: allNotesPosts[0]?.data.title },
];

const barMax = Math.max(1, ...barSource.map((b) => b.items.length));
const totalPosts = barSource.reduce((n, b) => n + b.items.length, 0);

const bars = barSource
    .map((b) => {
        const count = b.items.length;
        const ratio = barMax > 1 ? (count - 1) / (barMax - 1) : 1;
        return {
            name: b.name, en: b.en, unit: b.unit, href: b.href,
            count, latest: b.latest,
            // 含有全站最新一篇的那一欄用沙色標記，跟上方「最近更新」呼應
            newest: recent ? b.href.startsWith(`/${recent.kindEn.toLowerCase()}`) || recent.href.startsWith(b.href) : false,
            pct: count === 0 ? 0 : ((BAR_FLOOR + ratio * (100 - BAR_FLOOR)) * BAR_TOP) / 100,
        };
    })
    .sort((a, b) => b.count - a.count);

const slides = [
    {
        name: "生活隨筆", en: "JOURNAL", unit: "篇",
        href: "/blog", count: allJournalPosts.length,
        latest: allJournalPosts[0]?.data.title ?? null,
    },
    {
        name: "學習筆記", en: "NOTES", unit: "篇",
        href: "/notes", count: allNotesPosts.length,
        latest: allNotesPosts[0]?.data.title ?? null,
    },
    {
        name: "讀讀文獻", en: "RESEARCH", unit: "篇",
        href: "/research", count: allResearchPosts.length,
        latest: allResearchPosts[0]?.data.title ?? null,
    },
    {
        name: "看看書籍", en: "READING", unit: "本",
        href: "/reading", count: allReadingPosts.length,
        latest: allReadingPosts[0]?.data.title ?? null,
    },
    {
        name: "聽聽音樂", en: "MUSIC", unit: "首",
        href: "/music", count: allMusicPosts.length,
        latest: allMusicPosts[0]
            ? `${allMusicPosts[0].data.artist} — ${allMusicPosts[0].data.songName}`
            : null,
    },
    {
        name: "作品集", en: "PROJECTS", unit: "個",
        href: "/projects", count: allProjects.length,
        latest: allProjects[0]?.data.title ?? null,
    },
];
---

<Layout title="Bruce 個人網站">

    <!-- ══ HERO：上下各半，整頁高 ══ -->
    <section id="hero-section" class="hero-section">

        <!-- 上半：左邊身分、右邊最近更新 -->
        <div class="hero-top">

            <!-- 左：原本的標題與標語，改成靠左 -->
            <a href="/about" class="hero-id">
                <h1 class="banner-title" style="font-style: italic;">Bruce's Website</h1>
                <div id="slogan" class="banner-slogan">{randomSlogan}</div>
                <div class="hero-id-rule"></div>
                <div class="profile-cta">
                    <span class="cta-text">查看個人簡介</span>
                    <span class="cta-line"></span>
                </div>
            </a>

            <!-- 右：最近更新。圖片走退路階梯，最後一定有星空接住 -->
            <a href={recent?.href ?? "/blog"} class="hero-recent">
                <div class="hr-k">
                    <b>最近更新</b>
                    <span>{recent?.kind} · {recentDateStr}</span>
                </div>

                {recent?.image && recent.imageKind === "youtube" ? (
                    <div class="hr-media small">
                        <span class="hr-thumb">
                            <img
                                src={recent.image}
                                data-fallback={`https://img.youtube.com/vi/${recent.youtubeId}/hqdefault.jpg`}
                                alt=""
                                width="480" height="360"
                                loading="eager" fetchpriority="high"
                            />
                        </span>
                        <span class="hr-thumb-note">{recent.desc}</span>
                    </div>
                ) : recent?.image ? (
                    <div class="hr-media">
                        <img
                            src={recent.image}
                            alt=""
                            width="1800" height="1013"
                            loading="eager" fetchpriority="high"
                        />
                    </div>
                ) : (
                    <div class="hr-sky">
                        <canvas id="sky-canvas"></canvas>
                        <span class="hr-sky-cap">{skyPosts.length} POSTS</span>
                        <script
                            type="application/json"
                            id="sky-data"
                            is:inline
                            set:html={JSON.stringify(skyPosts)}
                        />
                    </div>
                )}

                <div class="hr-body">
                    <div class="hr-title">{recent?.title}</div>
                    {recent?.desc && recent.imageKind !== "youtube" && (
                        <div class="hr-desc">{recent.desc}</div>
                    )}
                </div>

                <div class="hr-go">
                    <span class="t">閱讀全文</span>
                    <span class="ar">&#8594;</span>
                </div>
            </a>
        </div>

        <!-- 下半：關於我 ＋ 各分類篇數長條 -->
        <div class="hero-bottom">

            <!-- 左：關於我 -->
            <a href="/about" class="db-about">
                <span class="db-hd">About · 關於我</span>
                <span class="db-frame">
                    <img src="/images/profile.jpg" alt="曾冠瑜" width="960" height="1705" loading="lazy" transition:name="profile-photo" />
                </span>
                <span class="db-nm">曾冠瑜</span>
                <span class="db-en">Bruce Tseng</span>
                <span class="db-go"><span>查看 個人簡介</span><span>&#8594;</span></span>
            </a>

            <!-- 右：長條。軌道等寬滿高可點，填充按篇數等比（最矮保底 14%）。
                 比例壓過了所以刻意不畫刻度線——格線對不上長條頂端，留著就是在騙人。 -->
            <div class="db-chart">
                <div class="db-head">
                    <b>{totalPosts}</b>
                    <span>posts across {bars.length} sections</span>
                    <span class="db-read" id="db-read"></span>
                </div>

                <div class="db-plot">
                    <div class="db-cols">
                        {bars.map((b) => (
                            <a
                                href={b.href}
                                class:list={["db-col", { newest: b.newest }]}
                                data-latest={b.latest ? `〈${b.latest}〉` : "尚無內容"}
                            >
                                <span class="db-fill" style={`height:${b.pct}%`}></span>
                            </a>
                        ))}
                    </div>
                    <div class="db-axis"></div>
                    <div class="db-labels">
                        {bars.map((b) => (
                            <span class="db-lab">
                                <span class="zh">{b.name}<span class="num">{b.count}<i>{b.unit}</i></span></span>
                                <span class="en">{b.en}</span>
                            </span>
                        ))}
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ══ 內容區：snap point，之後正常 scroll ══ -->
    <div id="content-section" class="content-section">

        {featuredProjects.length > 0 && (
            <section class="featured-projects">
                <div class="fp-header">
                    <div class="fp-title-group">
                        <span class="fp-label">SIDE PROJECTS</span>
                        <h2 class="fp-title">作品集 <span class="fp-sub">FEATURED WORKS</span></h2>
                    </div>
                    <a href="/projects" class="fp-view-all">查看全部 →</a>
                </div>
                <div class="fp-grid">
                    {featuredProjects.map((project: CollectionEntry<"projects">) => (
                        <a href={`/projects/${project.id}`} class="fp-card">
                            <div class="fp-cover">
                                {project.data.coverImage
                                    ? <img src={project.data.coverImage} alt={project.data.title} />
                                    : <div class="fp-cover-placeholder"><span>{project.data.techStack[0] || "?"}</span></div>
                                }
                                <span class={`fp-status fp-status--${project.data.status.toLowerCase()}`}>
                                    {project.data.status}
                                </span>
                            </div>
                            <div class="fp-info">
                                <h3 class="fp-name">{project.data.title}</h3>
                                <p class="fp-desc">{project.data.description}</p>
                                <div class="fp-tags">
                                    {project.data.techStack.slice(0, 3).map((tech: string) => (
                                        <span class="fp-tag">{tech}</span>
                                    ))}
                                </div>
                                <div class="fp-links">
                                    {project.data.demoUrl && <span class="fp-link-badge">Demo</span>}
                                    {project.data.githubUrl && <span class="fp-link-badge">GitHub</span>}
                                </div>
                            </div>
                        </a>
                    ))}
                </div>
            </section>
        )}

        <section class="featured-music">
            <div class="fp-header">
                <div class="fp-title-group">
                    <span class="fp-label">SOUND & TRACKS</span>
                    <h2 class="fp-title">聽聽音樂 <span class="fp-sub">RECENT PICKS</span></h2>
                </div>
                <a href="/music" class="fp-view-all">查看全部 →</a>
            </div>
            <div class="fm-grid">
                {allMusicPosts.slice(0, 3).map((post) => (
                    <a href={`/music/${post.id}`} class="fm-card">
                        <div class="fm-cover">
                            {post.data.youtubeId
                                ? <img src={`https://img.youtube.com/vi/${post.data.youtubeId}/hqdefault.jpg`} alt={post.data.songName} />
                                : <div class="fm-cover-placeholder"><span>{post.data.artist}</span></div>
                            }
                            <span class="fm-artist">{post.data.artist}</span>
                        </div>
                        <div class="fm-info">
                            <h3 class="fm-songname">《{post.data.songName}》</h3>
                            <p class="fm-desc">{post.data.title}</p>
                        </div>
                    </a>
                ))}
            </div>
        </section>

        <div class="quad-grid">
            <section class="home-section">
                <a href="/blog" class="section-title-link">
                    <div class="title-container">
                        <h2 class="section-title">生活隨筆<div class="hover-accent-line"></div></h2>
                        <span class="section-en">JOURNAL</span>
                    </div>
                    <span class="enter-arrow">→</span>
                </a>
                <div class="section-content">
                    {allJournalPosts.slice(0, 3).map((post) => (
                        <a href={`/blog/${post.id}`} class="preview-item">
                            <div class="preview-date">
                                <span class="p-year">{post.data.pubDate.getFullYear()}</span>
                                <span class="p-day">{post.data.pubDate.getDate()}日</span>
                            </div>
                            <div class="preview-body">
                                <h3 class="p-title">{post.data.title}</h3>
                                <p class="p-excerpt">{post.data.mood || "今日無事"}</p>
                            </div>
                        </a>
                    ))}
                    <div class="see-more-wrapper">
                        <a href="/blog" class="see-more-btn">查看更多 MORE<div class="btn-line"></div></a>
                    </div>
                </div>
            </section>

            <section class="home-section">
                <a href="/research" class="section-title-link">
                    <div class="title-container">
                        <h2 class="section-title">讀讀文獻<div class="hover-accent-line"></div></h2>
                        <span class="section-en">LITERATURE</span>
                    </div>
                    <span class="enter-arrow">→</span>
                </a>
                <div class="section-content">
                    {allResearchPosts.slice(0, 3).map((post) => (
                        <a href={`/research/${post.id}`} class="preview-item">
                            <div class="preview-date">
                                <span class="p-year">{post.data.pubDate.getFullYear()}</span>
                                <span class="p-day">{post.data.pubDate.getDate()}日</span>
                            </div>
                            <div class="preview-body">
                                <h3 class="p-title">{post.data.title}</h3>
                                <p class="p-excerpt">{post.data.category || "學術探究"}</p>
                            </div>
                        </a>
                    ))}
                    <div class="see-more-wrapper">
                        <a href="/research" class="see-more-btn">查看更多 MORE<div class="btn-line"></div></a>
                    </div>
                </div>
            </section>

            <section class="home-section">
                <a href="/reading" class="section-title-link">
                    <div class="title-container">
                        <h2 class="section-title">看看書籍<div class="hover-accent-line"></div></h2>
                        <span class="section-en">READING</span>
                    </div>
                    <span class="enter-arrow">→</span>
                </a>
                <div class="section-content">
                    {allReadingPosts.slice(0, 3).map((post) => (
                        <a href={`/reading/${post.id}`} class="preview-item">
                            <div class="preview-date">
                                <span class="p-year">{post.data.pubDate.getFullYear()}</span>
                                <span class="p-day">{post.data.pubDate.getDate()}日</span>
                            </div>
                            <div class="preview-body">
                                <h3 class="p-title">{post.data.title}</h3>
                                <p class="p-excerpt">{post.data.author || "書海拾貝"}</p>
                            </div>
                        </a>
                    ))}
                    <div class="see-more-wrapper">
                        <a href="/reading" class="see-more-btn">查看更多 MORE<div class="btn-line"></div></a>
                    </div>
                </div>
            </section>

            <section class="home-section">
                <a href="/notes" class="section-title-link">
                    <div class="title-container">
                        <h2 class="section-title">學習筆記<div class="hover-accent-line"></div></h2>
                        <span class="section-en">FIELD NOTES</span>
                    </div>
                    <span class="enter-arrow">→</span>
                </a>
                <div class="section-content">
                    {allNotesPosts.slice(0, 3).map((note) => (
                        <a href={`/notes/${note.id}`} class="preview-item">
                            <div class="preview-date">
                                <span class="p-year">{note.data.pubDate.getFullYear()}</span>
                                <span class="p-day">{note.data.pubDate.getDate()}日</span>
                            </div>
                            <div class="preview-body">
                                <h3 class="p-title">{note.data.title}</h3>
                                <p class="p-excerpt">{note.data.category || "學習筆記"}</p>
                            </div>
                        </a>
                    ))}
                    <div class="see-more-wrapper">
                        <a href="/notes" class="see-more-btn">查看更多 MORE<div class="btn-line"></div></a>
                    </div>
                </div>
            </section>
        </div>

    </div><!-- end content-section -->

</Layout>

<script is:inline define:vars={{ slogans, randomSlogan }}>
    var _cleanups = [];

    // ── 清除所有 cleanup ──────────────────────────────────────
    document.addEventListener("astro:before-preparation", function () {
        _cleanups.forEach(function (fn) { fn(); });
        _cleanups = [];
        document.documentElement.classList.remove("hp-snap");
    });

    // ── 字卡展開動畫：延遲 Astro 換頁，讓 cover 先跑完（只註冊一次）──
    if (!window._cardNavDelayAdded) {
        window._cardNavDelayAdded = true;
        document.addEventListener("astro:before-preparation", function (e) {
            if (!document.documentElement.classList.contains("card-nav")) return;
            var orig = e.loader;
            e.loader = function () {
                return new Promise(function (r) { setTimeout(r, 340); })
                    .then(function () { return orig(); });
            };
        });
    }

    // ── 換頁後清除 card-nav class ────────────────────────────────
    document.addEventListener("astro:page-load", function () {
        document.documentElement.classList.remove("card-nav");
    });

    // ── Scroll Snap 啟動 ──────────────────────────────────────
    function initScrollSnap() {
        var nav = document.querySelector(".sticky-nav");
        var navH = nav ? nav.offsetHeight : 50;
        document.documentElement.style.setProperty("--nav-h", navH + "px");
        document.documentElement.classList.add("hp-snap");

        // 視窗大小改變時重算
        function onResize() {
            var n = document.querySelector(".sticky-nav");
            var h = n ? n.offsetHeight : 50;
            document.documentElement.style.setProperty("--nav-h", h + "px");
        }
        window.addEventListener("resize", onResize);
        _cleanups.push(function () { window.removeEventListener("resize", onResize); });
    }

    // ── Carousel（撲克疊牌）────────────────────────────────────

    // ── 標語輪播 ──────────────────────────────────────────────
    function initSlogan() {
        var el = document.getElementById("slogan");
        if (window._sloganTimer) clearInterval(window._sloganTimer);
        if (!el) return;
        var idx = slogans.indexOf(randomSlogan);
        function next() {
            el.style.opacity = "0";
            setTimeout(function () {
                idx = (idx + 1) % slogans.length;
                el.textContent = slogans[idx];
                el.style.opacity = "1";
            }, 500);
        }
        window._sloganTimer = setInterval(next, 3000);
        _cleanups.push(function () { clearInterval(window._sloganTimer); });
    }

    // ── 入口 ──────────────────────────────────────────────────
    document.addEventListener("astro:page-load", function () {
        if (window.location.pathname !== "/") return;
        initScrollSnap();
        initSlogan();
    });
</script>

<style is:global>
    /* ── Scroll snap（僅首頁啟用）── */
    html.hp-snap {
        scroll-snap-type: y mandatory;
        scroll-padding-top: var(--nav-h, 50px);
    }
    html.hp-snap .hero-section    { scroll-snap-align: start; }
    html.hp-snap .content-section { scroll-snap-align: start; }

</style>

<style>
    /* ══ Layout ══════════════════════════════════════════════ */

    /* Hero section：全頁高 - navbar */
    .hero-section {
        height: calc(100dvh - var(--nav-h, 50px));
        display: flex;
        flex-direction: column;
        /* 突破 Layout 的 4rem 側邊距，做全寬 */
        margin-left:  -4rem;
        margin-right: -4rem;
        width: calc(100% + 8rem);
        overflow: hidden;
    }

    /* 內容區：snap 後正常 scroll */
    .content-section {
        padding-top: 2.5rem;
    }

    /* ══ Hero Top：左身分 / 右最近更新 ═══════════════════════ */
    .hero-top {
        flex: 0 0 54%;
        /* flex item 預設 min-height: auto，內容比 40% 高時不會縮，
           底部就會被 overflow: hidden 切掉。必須明確歸零。 */
        min-height: 0;
        position: relative;
        overflow: hidden;
        display: grid;
        grid-template-columns: 38% 1fr;
        background: var(--paper);
    }

    /* ── 左欄：原本的內容，改成靠左（不再置中）── */
    .hero-id {
        position: relative;
        z-index: 3;
        min-height: 0;
        display: flex;
        flex-direction: column;
        /* 內容垂直置中：區塊變高之後靠上會在下方留一個洞，
           置中則讓留白變成上下對稱的邊界。水平仍然靠左。 */
        justify-content: center;
        /* 4rem = 容器與 .nav-inner 的側邊距。hero 用 -4rem 突破容器，
           所以這裡補回 4rem，標題的 B 才會對齊導覽列的「首頁 / INDEX」。 */
        padding: 2.2rem 1.8rem 2.2rem 4rem;
        text-decoration: none;
        color: inherit;
    }
    .hero-id-rule {
        width: 46px; height: 2px;
        background: var(--sand);
        margin-top: 1.3rem;
    }

    /* ── 四角相框 ── */
    .frame-corner {
        position: absolute;
        width: 40px;
        height: 40px;
        border-color: var(--ink);
        border-style: solid;
        border-width: 0;
        z-index: 2;
        transition: width 0.4s ease, height 0.4s ease, border-color 0.4s;
    }
    .fc-tl { top: 1.4rem; left: 1.4rem;  border-top-width: 2px; border-left-width: 2px; }
    .fc-tr { top: 1.4rem; right: 1.4rem; border-top-width: 2px; border-right-width: 2px; }
    .fc-bl { bottom: 1.4rem; left: 1.4rem;  border-bottom-width: 2px; border-left-width: 2px; }
    .fc-br { bottom: 1.4rem; right: 1.4rem; border-bottom-width: 2px; border-right-width: 2px; }
    .banner-link-wrapper:hover .frame-corner { width: 56px; height: 56px; border-color: var(--accent); }

    /* ── 水印 ── */
    .banner-watermark {
        position: absolute;
        right: 6%;
        top: 50%;
        transform: translateY(-50%);
        font-family: "LXGW WenKai TC", serif;
        font-size: clamp(4rem, 10vw, 8rem);
        font-weight: 700;
        color: var(--ink);
        opacity: 0.05;
        letter-spacing: -4px;
        user-select: none;
        pointer-events: none;
        z-index: 1;
        white-space: nowrap;
    }

    .banner-title  { font-size: 2.6rem; margin: 0; font-weight: 700; letter-spacing: -1px; }
    .banner-slogan { font-size: 1.1rem; margin-top: 0.95rem; color: var(--muted); font-style: italic; letter-spacing: 0.08rem; transition: opacity 0.5s; }

    .profile-cta  { margin-top: 1.5rem; position: relative; padding-bottom: 7px; width: max-content; font-weight: bold; }
    .cta-text     { font-size: 0.78rem; font-family: var(--font-mono); color: var(--muted); letter-spacing: 2px; transition: color 0.4s; }
    .cta-line     { position: absolute; bottom: 0; left: 0; width: 34px; height: 1px; background: var(--sand); transition: width 0.4s; }
    .hero-id:hover .cta-text { color: var(--accent-deep); }
    .hero-id:hover .cta-line { width: 110px; }

    /* ── 右欄：最近更新 ── */
    .hero-recent {
        position: relative;
        z-index: 3;
        min-height: 0;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        padding: 1.7rem 2.2rem 1.6rem;
        border-left: 1px solid var(--hair);
        text-decoration: none;
        color: inherit;
        min-width: 0;
    }
    .hr-k {
        font-family: var(--font-mono); font-size: 0.68rem; letter-spacing: 2px;
        text-transform: uppercase; color: var(--muted);
        display: flex; gap: 0.9rem; align-items: baseline; flex-wrap: wrap;
    }
    .hr-k b { color: var(--accent-deep); font-weight: 500; }

    /* 照片給固定 16:9 的框，不滿版流血——面板 2.4:1、照片 1.78:1，
       滿版 cover 會切掉約 26% 的高度，而且沒辦法逐張調重心。 */
    .hr-media {
        margin-top: 0.75rem;
        aspect-ratio: 16 / 9;
        width: 100%;
        min-height: 0;
        flex: 0 1 auto;
        border: 1px solid var(--border);
        overflow: hidden;
        background: var(--paper-lift);
    }
    .hr-media img {
        width: 100%; height: 100%; object-fit: cover; display: block;
        filter: saturate(0.62) contrast(0.96) brightness(1.04);
        transition: filter 0.4s ease;
    }
    .hero-recent:hover .hr-media img { filter: none; }

    /* 解析度不夠的圖不要放大：YouTube hqdefault 只有 480×360 */
    .hr-media.small {
        aspect-ratio: auto; height: 104px;
        display: flex; align-items: center; gap: 1rem;
        padding: 0.85rem 1rem;
    }
    .hr-thumb { width: 148px; height: 76px; flex: none; overflow: hidden; border: 1px solid var(--hair); }
    .hr-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }
    .hr-thumb-note { font-size: 0.88rem; color: var(--muted); line-height: 1.6; }

    /* 沒有任何圖時的退路：星空 */
    .hr-sky {
        margin-top: 0.75rem; flex: 1 1 0; min-height: 48px; position: relative;
        border: 1px solid var(--border); background: var(--paper-lift); overflow: hidden;
    }
    .hr-sky canvas { display: block; width: 100%; height: 100%; }
    .hr-sky-cap {
        position: absolute; left: 0.75rem; bottom: 0.6rem;
        font-family: var(--font-mono); font-size: 0.6rem; letter-spacing: 2px;
        color: var(--muted);
    }

    .hr-body { margin-top: 0.85rem; }
    .hr-title { font-size: 1.3rem; line-height: 1.45; }
    .hr-desc {
        font-size: 0.92rem; color: var(--muted); margin-top: 0.45rem; line-height: 1.7;
        display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
    }
    .hr-go { margin-top: auto; padding-top: 0.8rem; display: flex; align-items: baseline; gap: 0.6rem; }
    .hr-go .t {
        font-family: var(--font-mono); font-size: 0.72rem; letter-spacing: 2px;
        text-transform: uppercase; color: var(--accent-deep);
    }
    .hr-go .ar { color: var(--accent-deep); font-size: 0.85rem; transition: transform 0.3s; }
    .hero-recent:hover .hr-go .ar { transform: translateX(5px); }

    /* ══ Hero Bottom：關於我 ＋ 篇數長條 ═══════════════════════ */
    .hero-bottom {
        flex: 0 0 46%;
        min-height: 0;
        display: grid;
        grid-template-columns: 25% 1fr;
        background: var(--paper);
        border-top: 1px solid var(--border);
        overflow: hidden;
    }

    /* ── 左：關於我 ── */
    .db-about {
        border-right: 1px solid var(--hair);
        padding: 1.3rem 1.4rem 1.2rem 4rem;
        display: flex; flex-direction: column; align-items: center; text-align: center;
        text-decoration: none; color: inherit;
        min-width: 0; min-height: 0; overflow: hidden;
    }
    .db-hd {
        font-family: var(--font-mono); font-size: 0.62rem; letter-spacing: 2px;
        text-transform: uppercase; color: var(--muted); align-self: flex-start;
    }
    /* 相框比例鎖死 3:4：照片是 960×1705 直式，框一旦變橫臉就會被裁掉 */
    .db-frame {
        margin-top: auto; aspect-ratio: 3 / 4; height: 50%; width: auto;
        padding: 5px; background: var(--paper-lift); border: 1px solid var(--border);
        transform: rotate(-1.5deg); position: relative;
        transition: transform 0.35s ease, box-shadow 0.35s ease;
        box-shadow: 3px 3px 0 color-mix(in srgb, var(--accent) 8%, transparent),
                    0 8px 22px color-mix(in srgb, var(--accent) 12%, transparent);
    }
    .db-about:hover .db-frame { transform: rotate(0deg) scale(1.03); }
    .db-frame img { width: 100%; height: 100%; object-fit: cover; object-position: center top; display: block; }
    .db-nm { margin-top: 0.75rem; font-size: 1.05rem; }
    .db-en { font-family: var(--font-mono); font-size: 0.66rem; letter-spacing: 3px; color: var(--muted); margin-top: 0.25rem; }
    .db-go {
        margin-top: auto; padding-top: 0.75rem; align-self: stretch;
        display: flex; align-items: baseline; justify-content: space-between;
        font-family: var(--font-mono); font-size: 0.68rem; letter-spacing: 2px;
        text-transform: uppercase; color: var(--accent-deep);
    }
    .db-go span:last-child { transition: transform 0.3s; }
    .db-about:hover .db-go span:last-child { transform: translateX(4px); }

    /* ── 右：長條 ── */
    .db-chart {
        position: relative;
        padding: 1.25rem 2rem 1rem;
        /* grid 的 minmax(0,1fr) 不會像 flex:1 那樣被內容撐開 */
        display: grid; grid-template-rows: auto minmax(0, 1fr);
        min-width: 0; min-height: 0;
    }
    .db-head {
        font-family: var(--font-mono); font-size: 0.62rem; letter-spacing: 2px;
        text-transform: uppercase; color: var(--muted);
        display: flex; gap: 1rem; align-items: baseline;
    }
    .db-head b { color: var(--ink); font-weight: 500; }
    /* 共用讀數：滑過哪一欄就顯示那一區最新一篇。
       放頂端所以永遠不會跟長條或數字打架。 */
    .db-read {
        margin-left: auto; font-family: "LXGW WenKai TC", serif; font-size: 0.8rem;
        color: var(--muted); text-transform: none; letter-spacing: 0;
        opacity: 0; transition: opacity 0.22s;
        white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 46%;
    }
    .db-chart:hover .db-read { opacity: 1; }

    .db-plot { position: relative; min-height: 0; margin-top: 0.9rem; }
    .db-cols { position: absolute; inset: 0 0 46px; display: flex; gap: 10px; align-items: stretch; }
    .db-col {
        flex: 1; min-width: 0; position: relative;
        display: flex; flex-direction: column; justify-content: flex-end;
        text-decoration: none; color: inherit;
        background: color-mix(in srgb, var(--haze) 26%, transparent);
        transition: background 0.25s;
    }
    .db-col:hover { background: color-mix(in srgb, var(--haze) 55%, transparent); }
    .db-fill {
        background: color-mix(in srgb, var(--accent) 26%, transparent);
        border-top: 2px solid var(--accent);
        position: relative;
    }
    .db-col.newest .db-fill { border-top-color: var(--sand); }
    .db-col.newest .db-fill::after {
        content: ''; position: absolute; right: 6px; top: -9px;
        width: 5px; height: 5px; background: var(--sand);
    }
    .db-axis { position: absolute; left: 0; right: 0; bottom: 46px; height: 1px; background: var(--ink); }
    .db-labels { position: absolute; left: 0; right: 0; bottom: 0; height: 46px; display: flex; gap: 10px; }
    .db-lab { flex: 1; min-width: 0; padding-top: 6px; text-align: center; }
    .db-lab .zh { font-size: 0.72rem; display: block; }
    .db-lab .num {
        font-family: var(--font-mono); font-size: 1.12rem; color: var(--ink);
        font-variant-numeric: tabular-nums; margin-left: 6px;
    }
    .db-lab .num i { font-style: normal; font-size: 0.62rem; color: var(--muted); margin-left: 2px; }
    .db-lab .en { font-family: var(--font-mono); font-size: 0.56rem; letter-spacing: 2px; color: var(--muted); display: block; margin-top: 3px; }


    /* ══ Content Section（以下沿用既有樣式）════════════════════ */
    .featured-projects { margin-bottom: 3rem; }
    .fp-header { display: flex; justify-content: space-between; align-items: flex-end; margin-bottom: 1.2rem; padding-bottom: 0.7rem; border-bottom: 1px solid var(--border); }
    .fp-label  { font-size: 0.6rem; font-family: var(--font-mono); letter-spacing: 4px; color: var(--accent-deep); display: block; margin-bottom: 0.3rem; text-transform: uppercase; }
    .fp-title  { font-size: 1.4rem; margin: 0; font-weight: 700; }
    .fp-sub    { font-size: 0.7rem; font-family: var(--font-mono); letter-spacing: 3px; color: var(--muted); font-weight: 400; margin-left: 0.5rem; }
    .fp-view-all { font-size: 0.75rem; font-family: var(--font-mono); letter-spacing: 2px; color: var(--muted); text-decoration: none; transition: color 0.3s; }
    .fp-view-all:hover { color: var(--accent-deep); }

    .fp-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.2rem; }
    .fp-card  { text-decoration: none; color: inherit; border: 1px solid var(--border); border-radius: 4px; overflow: hidden; transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s; display: flex; flex-direction: column; }
    .fp-card:hover { transform: translateY(-4px); box-shadow: 0 8px 24px color-mix(in srgb, var(--accent) 10%, transparent); border-color: var(--accent); }

    .fp-cover { position: relative; height: 140px; overflow: hidden; background: var(--border); }
    .fp-cover img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s; }
    .fp-card:hover .fp-cover img { transform: scale(1.04); }
    .fp-cover-placeholder { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; background: linear-gradient(135deg,color-mix(in srgb, var(--accent) 8%, transparent),color-mix(in srgb, var(--accent) 18%, transparent)); }
    .fp-cover-placeholder span { font-family: var(--font-mono); font-size: 0.75rem; letter-spacing: 3px; color: var(--accent-deep); text-transform: uppercase; font-weight: 700; }
    .fp-status { position: absolute; top: 0.6rem; right: 0.6rem; font-size: 0.6rem; font-family: var(--font-mono); letter-spacing: 1px; padding: 2px 7px; border-radius: 20px; font-weight: 700; }
    .fp-status--active   { background: rgba(79,125,100,.16); color: #4f7d64; }
    .fp-status--wip      { background: rgba(151,113,63,.16); color: #97713f; }
    .fp-status--archived { background: rgba(125,143,150,.16);color: #7d8f96; }
    .fp-info { padding: 0.9rem; display: flex; flex-direction: column; gap: 0.4rem; flex: 1; }
    .fp-name { font-size: 1rem; font-weight: 700; margin: 0; }
    .fp-desc { font-size: 0.78rem; color: var(--muted); margin: 0; line-height: 1.5; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
    .fp-tags { display: flex; gap: 0.4rem; flex-wrap: wrap; margin-top: auto; }
    .fp-tag  { font-size: 0.62rem; font-family: var(--font-mono); letter-spacing: 1px; padding: 2px 7px; border: 1px solid var(--border); border-radius: 3px; color: var(--muted); }
    .fp-links { display: flex; gap: 0.5rem; }
    .fp-link-badge { font-size: 0.6rem; font-family: var(--font-mono); letter-spacing: 1px; padding: 2px 7px; background: color-mix(in srgb, var(--accent) 8%, transparent); color: var(--accent-deep); border-radius: 3px; }

    .featured-music { margin-bottom: 3rem; }
    .fm-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.2rem; }
    .fm-card { text-decoration: none; color: inherit; border: 1px solid var(--border); border-radius: 4px; overflow: hidden; transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s; display: flex; flex-direction: column; }
    .fm-card:hover { transform: translateY(-4px); box-shadow: 0 8px 24px color-mix(in srgb, var(--accent) 10%, transparent); border-color: var(--accent); }
    .fm-cover { position: relative; height: 190px; overflow: hidden; background: var(--border); }
    .fm-cover img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s; }
    .fm-card:hover .fm-cover img { transform: scale(1.04); }
    .fm-cover-placeholder { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; background: linear-gradient(135deg,color-mix(in srgb, var(--accent) 8%, transparent),color-mix(in srgb, var(--accent) 18%, transparent)); }
    .fm-cover-placeholder span { font-family: var(--font-mono); font-size: 0.75rem; letter-spacing: 3px; color: var(--accent-deep); text-transform: uppercase; font-weight: 700; }
    .fm-artist { position: absolute; top: 0.6rem; right: 0.6rem; font-size: 0.6rem; font-family: var(--font-mono); letter-spacing: 1px; padding: 2px 8px; border-radius: 20px; font-weight: 700; background: color-mix(in srgb, var(--accent) 18%, transparent); color: var(--accent-deep); }
    .fm-info { padding: 0.9rem; display: flex; flex-direction: column; gap: 0.3rem; flex: 1; }
    .fm-songname { font-size: 1rem; font-weight: 700; margin: 0; }
    .fm-desc { font-size: 0.78rem; color: var(--muted); margin: 0; line-height: 1.5; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }

    .quad-grid { display: grid; grid-template-columns: 1fr; gap: 2rem; }
    .section-title-link { display: flex; justify-content: space-between; align-items: flex-end; text-decoration: none; color: var(--ink); padding-bottom: 0.7rem; border-bottom: 1px solid var(--border); margin-bottom: 1.2rem; }
    .title-container { position: relative; display: flex; align-items: baseline; gap: 1.2rem; }
    .section-en { margin-left: 0.5rem; }
    .section-title { font-size: 1.7rem; margin: 0; transition: transform 0.4s, color 0.4s; position: relative; }
    .hover-accent-line { position: absolute; bottom: -6px; left: 0; width: 0; height: 3px; background: var(--accent); transition: width 0.35s ease; }
    .enter-arrow { font-size: 1rem; color: var(--muted); opacity: 0; transform: translateX(-10px); transition: 0.4s; }
    .section-title-link:hover .section-title { transform: translateX(10px); color: var(--accent-deep); }
    .section-title-link:hover .hover-accent-line { width: 100%; transition-delay: 0.25s; }
    .section-title-link:hover .enter-arrow { opacity: 1; transform: translateX(0); color: var(--accent-deep); }

    .preview-item { display: flex; gap: 1.2rem; text-decoration: none; color: inherit; padding: 0.55rem 0.6rem; border-bottom: 1px dashed var(--border); transition: 0.3s; }
    .preview-item:hover { background: color-mix(in srgb, var(--accent) 3%, transparent); transform: translateX(8px); }
    .preview-date { min-width: 52px; text-align: right; border-right: 1px solid var(--border); padding-right: 0.8rem; }
    .p-year { font-size: 0.6rem; color: var(--muted); display: block; }
    .p-day  { font-size: 0.9rem; font-weight: 700; }
    .preview-body { flex: 1; min-width: 0; }
    .p-title  { font-size: 0.9rem; font-weight: 700; margin: 0 0 0.2rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
    .p-excerpt { font-size: 0.75rem; color: var(--muted); margin: 0; }

    .see-more-wrapper { display: flex; justify-content: flex-end; margin-top: 0.8rem; }
    .see-more-btn { text-decoration: none; color: var(--muted); font-size: 0.7rem; letter-spacing: 2px; position: relative; padding: 8px 0; transition: color 0.3s; }
    .btn-line { position: absolute; bottom: 0; right: 0; width: 30px; height: 2px; background: var(--border); transition: 0.4s; }
    .see-more-btn:hover { color: var(--accent-deep); }
    .see-more-btn:hover .btn-line { width: 100%; background: var(--accent); }

    /* ══ Responsive ══════════════════════════════════════════ */
    @media (max-width: 900px) {
        /* 兩欄擠不下就疊起來，並把 hero 上半放高一點容納兩塊 */
        .hero-top      { grid-template-columns: 1fr; grid-template-rows: auto 1fr; flex-basis: 62%; }
        .hero-id       { padding: 1.4rem 1.4rem 1rem 4rem; }
        .hero-id-rule  { margin-top: 0.9rem; }
        .profile-cta   { margin-top: 0.9rem; }
        .hero-bottom   { flex-basis: 38%; }
        .hero-recent   { border-left: 0; border-top: 1px solid var(--hair); padding: 1rem 1.4rem 1.2rem; }
        .hr-desc       { -webkit-line-clamp: 1; }
        /* 下半：關於我改成橫向一列，長條在下 */
        .hero-bottom   { grid-template-columns: 1fr; grid-template-rows: auto 1fr; flex-basis: 38%; }
        .db-about      { flex-direction: row; align-items: center; gap: 1rem;
                         text-align: left; border-right: 0; border-bottom: 1px solid var(--hair);
                         padding: 0.8rem 1.4rem 0.8rem 4rem; }
        .db-hd         { display: none; }
        .db-frame      { margin: 0; height: 62px; }
        .db-nm         { margin-top: 0; }
        .db-go         { margin-left: auto; align-self: center; padding-top: 0; width: auto; gap: 0.6rem; }
        .db-chart      { padding: 0.9rem 1.4rem 0.8rem; }
    }
    @media (max-width: 768px) {
        .hero-section  { margin-left: -1rem; margin-right: -1rem; width: calc(100% + 2rem); }
        .fp-grid       { grid-template-columns: 1fr; }
        .fm-grid       { grid-template-columns: 1fr; }
        .banner-title  { font-size: 1.9rem; }
        .hero-id       { padding-left: 1rem; }
        .hero-recent   { padding-left: 1rem; padding-right: 1rem; }
        .db-about      { padding-left: 1rem; }
        .db-chart      { padding-left: 1rem; padding-right: 1rem; }
        .db-lab .zh    { font-size: 0.62rem; }
        .db-lab .num   { font-size: 0.88rem; margin-left: 3px; }
        .db-lab .en    { display: none; }
        .db-cols       { inset: 0 0 32px; gap: 6px; }
        .db-axis       { bottom: 32px; }
        .db-labels     { height: 32px; gap: 6px; }
        .db-read       { display: none; }
        .hr-media.small { height: 84px; gap: 0.7rem; }
        .hr-thumb      { width: 108px; height: 60px; }
        .hr-thumb-note { font-size: 0.7rem; }
    }
    @media (max-width: 500px) {
        .banner-slogan { display: none; }
        .hr-desc       { display: none; }
    }
</style>

<script>
    // ══ 星空：一顆星 = 一篇文章 ══════════════════════════════
    // 只在「最近更新沒有任何圖片可用」時才會出現在 DOM 裡。
    function initSky() {
        const cv = document.getElementById("sky-canvas") as HTMLCanvasElement | null;
        const raw = document.getElementById("sky-data");
        if (!cv || !raw) return;

        let posts: { c: string; t: number; k: string }[] = [];
        try { posts = JSON.parse(raw.textContent || "[]"); } catch { return; }
        if (!posts.length) return;

        // 位置由標題雜湊決定，所以每次載入都在同一個地方，不會亂跳
        const hash = (str: string) => {
            let h = 2166136261;
            for (let i = 0; i < str.length; i++) { h ^= str.charCodeAt(i); h = (h * 16777619) >>> 0; }
            return h;
        };
        const rnd = (seed: number, n: number) => {
            const out: number[] = []; let h = seed;
            for (let i = 0; i < n; i++) { h = (h * 1103515245 + 12345) >>> 0; out.push(h / 4294967296); }
            return out;
        };

        const times = posts.map((p) => p.t);
        const tMin = Math.min(...times), tMax = Math.max(...times);
        const span = Math.max(1, tMax - tMin);

        const stars = posts.map((p) => {
            const r = rnd(hash(p.c + p.k), 3);
            const age = (p.t - tMin) / span;
            return {
                c: p.c,
                x: 0.06 + r[0] * 0.88,
                y: 0.08 + r[1] * 0.84,
                mag: 0.35 + Math.pow(age, 1.8) * 0.65,
                phase: r[2] * Math.PI * 2,
                newest: age > 0.999,
            };
        });

        const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
        let raf = 0;

        const paint = (t: number) => {
            const dpr = Math.min(window.devicePixelRatio || 1, 2);
            const w = cv.clientWidth, h = cv.clientHeight;
            if (!w || !h) return;
            if (cv.width !== w * dpr) { cv.width = w * dpr; cv.height = h * dpr; }
            const g = cv.getContext("2d");
            if (!g) return;
            g.setTransform(dpr, 0, 0, dpr, 0, 0);
            g.clearRect(0, 0, w, h);

            const cs = getComputedStyle(document.documentElement);
            const ink = cs.getPropertyValue("--ink").trim() || "#141a1c";
            const soft = cs.getPropertyValue("--muted-soft").trim() || "#8fa4ab";
            const accent = cs.getPropertyValue("--accent").trim() || "#4e8496";

            const byCat: Record<string, typeof stars> = {};
            stars.forEach((st) => { (byCat[st.c] = byCat[st.c] || []).push(st); });
            g.lineWidth = 0.6;
            g.strokeStyle = soft;
            g.globalAlpha = 0.16;
            Object.keys(byCat).forEach((k) => {
                const list = byCat[k].slice().sort((a, b) => a.x - b.x);
                for (let i = 1; i < list.length; i++) {
                    g.beginPath();
                    g.moveTo(list[i - 1].x * w, list[i - 1].y * h);
                    g.lineTo(list[i].x * w, list[i].y * h);
                    g.stroke();
                }
            });
            g.globalAlpha = 1;

            stars.forEach((st) => {
                const tw = reduce ? 1 : 0.82 + 0.18 * Math.sin(t / 1400 + st.phase);
                const r = (0.9 + st.mag * 2.1) * tw;
                g.beginPath();
                g.arc(st.x * w, st.y * h, r, 0, Math.PI * 2);
                g.fillStyle = st.newest ? accent : ink;
                g.globalAlpha = (0.28 + st.mag * 0.6) * tw;
                g.fill();
                if (st.newest) {
                    g.beginPath();
                    g.arc(st.x * w, st.y * h, r + 4.5, 0, Math.PI * 2);
                    g.strokeStyle = accent;
                    g.globalAlpha = 0.3 * tw;
                    g.lineWidth = 1;
                    g.stroke();
                }
            });
            g.globalAlpha = 1;
        };

        const loop = (t: number) => { paint(t); raf = requestAnimationFrame(loop); };
        if (raf) cancelAnimationFrame(raf);
        if (reduce) paint(0); else raf = requestAnimationFrame(loop);

        document.addEventListener("astro:before-swap", () => {
            if (raf) cancelAnimationFrame(raf);
        }, { once: true });
    }

    // ══ YouTube maxresdefault 不是每支影片都有，404 就退回 hqdefault ══
    function initThumbFallback() {
        document.querySelectorAll<HTMLImageElement>(".hr-thumb img[data-fallback]").forEach((img) => {
            img.addEventListener("error", () => {
                const fb = img.dataset.fallback;
                if (fb && img.src !== fb) img.src = fb;
            }, { once: true });
        });
    }

    // ══ 儀表板：頂端共用讀數 ══════════════════════════════════
    // 滑過哪一欄就在標題列右側顯示那一區最新一篇。
    // 六個各自浮動的提示會互相打架，最高那根上方也沒有空間放。
    function initBarReadout() {
        const read = document.getElementById("db-read");
        const chart = document.querySelector(".db-chart");
        if (!read || !chart) return;
        chart.querySelectorAll<HTMLElement>(".db-col").forEach((col) => {
            col.addEventListener("mouseenter", () => {
                read.textContent = col.dataset.latest || "";
            });
        });
        chart.addEventListener("mouseleave", () => { read.textContent = ""; });
    }

    document.addEventListener("astro:page-load", () => {
        initSky();
        initThumbFallback();
        initBarReadout();
    });
</script>
